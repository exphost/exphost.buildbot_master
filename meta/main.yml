galaxy_info:
  author: TORGiren
  description: Buildbot master role
  company: exphost
  licence: MIT
  min_ansible_version: 2.8
  platforms:
    - name: centos
      versions:
        - 7
  galaxy_tags:
    - fabsible
    - buildbot

dependencies:
  - role: exphost.create_user
    user: "{{app.value.buildbot_master.user}}"
